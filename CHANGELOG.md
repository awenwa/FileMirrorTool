# FileMirrorTool 更新日志

## v1.2 (2026-06-12)

### 新增
- 方案管理功能：保存/载入/删除同步方案，方案列表显示在左侧面板
- 右键菜单：载入方案、再次同步、删除方案
- 方案配置自动保存到软件目录下 `config/` 子文件夹
- 方案执行后自动更新最后执行时间

### 改进
- 左右面板可拖动调整大小（PanedWindow）
- 文件统计阶段实时输出进度（每50个目录输出一次），避免界面"未响应"感知
- 同步方式改名为 Link（符号链接）/ Mirror（镜像复制）/ Mix（条件同步）
- 条件同步支持逻辑运算符（必须同时满足/满足任一/排除此类文件）
- 帮助按钮移至右上角最小化旁

### 修复
- Mix 模式保存方案时文件大小规则丢失
- 关于窗口未居中显示
- 符号链接文件名不完整
